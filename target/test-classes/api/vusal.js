
user: {
    wid : 'test1',
    role: 'HO_ADMIN',
    passw: 'asdf;lkj'
}
// fn(users, userType) {
if(user[userType] | user.role[userType]) return user;
user['HO_ADMIN']; user; user.role['HO_ADMIN'];



userType;// role
if(user.role === userType) return user;
if(user.role[userType]) return user;

if(user[userType]) return user;// src-test-users users.json.user.role['HO_ADMIN'] 
if(user.userType ) return user;//
//---***********************------
if(userType.includes('Home Office Manager')) userType = '[HO_ADMIN]';


if(user[role[HO_ADMIN]]) return user;//false
Given I am logged in to NMC policy launch as a role user
if(user.role == userType) return user;//true
